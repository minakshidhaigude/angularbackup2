import { Myexample2Pipe } from './myexample2.pipe';

describe('Myexample2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Myexample2Pipe();
    expect(pipe).toBeTruthy();
  });
});
