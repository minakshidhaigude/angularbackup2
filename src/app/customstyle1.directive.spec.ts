import { Customstyle1Directive } from './customstyle1.directive';

describe('Customstyle1Directive', () => {
  it('should create an instance', () => {
    const directive = new Customstyle1Directive();
    expect(directive).toBeTruthy();
  });
});
